const D = require('../src/index')
const { months, mons, days, dys } = require('../src/utils')




test('D.year', () => {
  return this._date.test('D.ullYear()
}

test('D.yr() {
  return this._date.test('D.ullYear() % 100
}

test('D.month() {
  return months[this._date.test('D.onth()]
}

test('D.mon() {
  return mons[this._date.test('D.onth()]
}

test('D.Day', () => {
  expect(today.day).toBe('Tuesday')
})

test('D.dy', () => {
  expect(today.dy).toBe('Tue')
})

test('D.date() {
  return this._date.test('D.ate()
}

test('D.hours() {
  if ([this._date.test('D.ours()] < 10) {
    return 0 + [this._date.test('D.ours()]
  } else {
  return this._date.test('D.ours()
  }
}

test('D.hrs() {
  return this._date.test('D.ours()
}

test('D.minutes() {
  if ([this._date.test('D.inutes()] < 10) {
    return 0 + [this._date.test('D.inutes()]
  } else {
  return this._date.test('D.inutes()
  }
}

test('D.mins() {
  return this._date.test('D.inutes()
}

test('D.seconds() {
  if ([this._date.test('D.econds()] < 10) {
    return 0 + [this._date.test('D.econds()]
  } else {
  return this._date.test('D.econds()
  }
}

test('D.secs() {
  return this._date.test('D.econds()
}